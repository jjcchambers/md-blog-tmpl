{
  "name": "md-blog-tmpl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A lightweight markdown blog template, deployed to Cloudflare Workers.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler deploy",
    "cf-preview": "astro build && wrangler dev"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.1.5"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
